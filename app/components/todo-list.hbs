{{#each @list as |todo|}}
    <div class="well">
        <h4>{{#link-to 'todos.edit' todo.id}}{{todo.title}}{{/link-to}} <span class="label label-primary pull-right">{{moment-from-now todo.date}}</span></h4>
        <p>{{todo.body}}</p>
    </div>
{{/each}}