{{#each @list as |todo|}}
    <div class="well">
        <h4>{{#link-to 'todos.edit' todo.id}}{{todo.title}}{{/link-to}} <small class="label label-primary">Deadline: <strong>{{moment-format todo.date 'DD/MM/YYYY'}}</strong></small></h4>
        <p>{{todo.body}}</p>
    </div>
{{/each}}